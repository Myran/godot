# SCsub
Import('env')

print("Firebase SCsub: platform=" + env['platform'] + ", arch=" + env['arch'])

# Shared C++ source files (all platforms)
# These contain platform-agnostic logic
env.add_source_files(env.modules_sources, "analytics.cpp")       # Pure C++ - works on all platforms
env.add_source_files(env.modules_sources, "auth.cpp")           # Pure C++ - works on all platforms
env.add_source_files(env.modules_sources, "convertor.cpp")
env.add_source_files(env.modules_sources, "database.cpp")
env.add_source_files(env.modules_sources, "firebase_common.cpp") # Shared Firebase logic
env.add_source_files(env.modules_sources, "firestore.cpp")
env.add_source_files(env.modules_sources, "messaging.cpp")
env.add_source_files(env.modules_sources, "register_types.cpp")
env.add_source_files(env.modules_sources, "remote_config.cpp")

# Platform-specific Firebase initialization (createApplication, quit_app)
if env['platform'] == 'windows':
    # Windows: Use C++ implementation (MSVC can't compile .mm)
    env.add_source_files(env.modules_sources, "firebase_windows.cpp")
else:
    # Android/iOS/macOS: Use Objective-C++ for platform-specific APIs
    env.add_source_files(env.modules_sources, "firebase_platform.mm")

env.Append(CPPPATH="#/../firebase/firebase_cpp_sdk/include")

if env['platform'] == 'iphone':
    print("Firebase: iOS platform")
    # Link Firebase C++ SDK libraries for iOS
    env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/ios/device-arm64/libfirebase_app.a")])
    env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/ios/device-arm64/libfirebase_analytics.a")])
    env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/ios/device-arm64/libfirebase_remote_config.a")])
    env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/ios/device-arm64/libfirebase_database.a")])
    env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/ios/device-arm64/libfirebase_firestore.a")])
    env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/ios/device-arm64/libfirebase_auth.a")])
    env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/ios/device-arm64/libfirebase_functions.a")])
    env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/ios/device-arm64/libfirebase_messaging.a")])

elif env['platform'] == 'macos':
    print("Firebase: macOS platform")
    # Link Firebase C++ SDK libraries for macOS (darwin)
    # SystemConfiguration framework required by Firestore (SCNetworkReachability symbols)
    env.Append(LINKFLAGS=['-framework', 'SystemConfiguration'])
    # Security framework required for cryptographic operations
    env.Append(LINKFLAGS=['-framework', 'Security'])
    # CoreFoundation required by various Firebase components
    env.Append(LINKFLAGS=['-framework', 'CoreFoundation'])
    # GSS framework required by curl for Kerberos authentication (from-source build)
    env.Append(LINKFLAGS=['-framework', 'GSS'])

    # OpenSSL/BoringSSL linking (required for from-source Firebase SDK builds)
    # The from-source build uses libcurl which links against system OpenSSL
    import os
    # Try Homebrew OpenSSL locations (ARM64 and Intel)
    openssl_paths = [
        '/opt/homebrew/opt/openssl/lib',  # ARM64 Homebrew
        '/usr/local/opt/openssl/lib',      # Intel Homebrew
        '/opt/homebrew/opt/openssl@3/lib', # Homebrew OpenSSL 3.x
        '/usr/local/opt/openssl@3/lib',    # Intel Homebrew OpenSSL 3.x
    ]
    openssl_found = False
    for openssl_path in openssl_paths:
        if os.path.exists(os.path.join(openssl_path, 'libssl.a')):
            print("Firebase: Using OpenSSL from " + openssl_path)
            env.Prepend(LIBS=[File(os.path.join(openssl_path, 'libssl.a'))])
            env.Prepend(LIBS=[File(os.path.join(openssl_path, 'libcrypto.a'))])
            openssl_found = True
            break
    if not openssl_found:
        print('WARN: OpenSSL not found. Firebase SDK from-source builds may fail to link.')
        print('      Install OpenSSL via: brew install openssl')

    if env['arch'] == 'arm64':
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/darwin/arm64/libfirebase_app.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/darwin/arm64/libfirebase_analytics.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/darwin/arm64/libfirebase_remote_config.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/darwin/arm64/libfirebase_database.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/darwin/arm64/libfirebase_firestore.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/darwin/arm64/libfirebase_auth.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/darwin/arm64/libfirebase_functions.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/darwin/arm64/libfirebase_messaging.a")])
    elif env['arch'] == 'x86_64':
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/darwin/x86_64/libfirebase_app.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/darwin/x86_64/libfirebase_analytics.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/darwin/x86_64/libfirebase_remote_config.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/darwin/x86_64/libfirebase_database.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/darwin/x86_64/libfirebase_firestore.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/darwin/x86_64/libfirebase_auth.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/darwin/x86_64/libfirebase_functions.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/darwin/x86_64/libfirebase_messaging.a")])
    else:
        print('WARN: Unsupported architecture for Firebase macOS: ' + env['arch'])

elif env['platform'] == 'android':
    print("Firebase: Android platform")
    if env['arch'] == 'arm32':
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/android/armeabi-v7a/libfirebase_app.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/android/armeabi-v7a/libfirebase_analytics.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/android/armeabi-v7a/libfirebase_remote_config.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/android/armeabi-v7a/libfirebase_database.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/android/armeabi-v7a/libfirebase_firestore.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/android/armeabi-v7a/libfirebase_auth.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/android/armeabi-v7a/libfirebase_functions.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/android/armeabi-v7a/libfirebase_messaging.a")])
    elif env['arch'] == 'arm64':
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/android/arm64-v8a/libfirebase_app.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/android/arm64-v8a/libfirebase_analytics.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/android/arm64-v8a/libfirebase_remote_config.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/android/arm64-v8a/libfirebase_database.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/android/arm64-v8a/libfirebase_firestore.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/android/arm64-v8a/libfirebase_auth.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/android/arm64-v8a/libfirebase_functions.a")])
        env.Prepend(LIBS=[File("#/../firebase/firebase_cpp_sdk/libs/android/arm64-v8a/libfirebase_messaging.a")])
    else:
        print('WARN: Unsupported architecture for firebase module: '+env['android_arch'])

elif env['platform'] == 'windows':
    print("Firebase: Windows platform")
    # Link Firebase C++ SDK libraries for Windows (MSVC)
    # Libraries built with VS2019/MT (static runtime) for x64
    # NOTE: Always use Release libs - Debug libs (/MTd) incompatible with Godot's debug_crt=/MDd (Task-434)
    if env['arch'] == 'x86_64':
        import os
        import glob

        lib_variant = 'Release'
        print("Firebase: Using " + lib_variant + " libraries for all Windows targets")

        lib_base = "#/../firebase/firebase_cpp_sdk/libs/windows/VS2019/MT/x64/" + lib_variant + "/"
        # Use SCons Dir to get absolute path (can't use __file__ in SConscript)
        module_dir = Dir('.').abspath  # This SCsub's directory
        lib_base_abs = os.path.join(module_dir, "..", "..", "..", "firebase", "firebase_cpp_sdk", "libs", "windows", "VS2019", "MT", "x64", lib_variant)
        print("Firebase DEBUG: lib_base_abs = " + lib_base_abs)
        print("Firebase DEBUG: lib_base_abs exists = " + str(os.path.exists(lib_base_abs)))

        # Check if deps/ directory exists (from-source build)
        deps_dir = os.path.join(lib_base_abs, "deps")
        print("Firebase DEBUG: deps_dir = " + deps_dir)
        print("Firebase DEBUG: deps_dir exists = " + str(os.path.exists(deps_dir)))

        # NOTE: Dynamic loading disabled - we use explicit deps section below for reliability.
        # The explicit section provides proper control over link order.
        if not os.path.exists(deps_dir):
            print("Firebase: WARNING - No deps/ directory found!")
            print("  Expected: " + deps_dir)
            print("  If using from-source Firebase SDK build, ensure:")
            print("    1. Build completed successfully")
            print("    2. Dependencies were copied to deps/ folder")
            print("  This WILL cause linker errors for missing symbols")

        # ============================================================================
        # EXPLICIT DEPENDENCY LIBRARIES (from-source Firebase SDK build)
        # These are transitive dependencies that SCons cannot automatically discover.
        # Listed explicitly for reliability (dynamic loading was unreliable on Windows).
        #
        # LINK ORDER: MSVC resolves symbols left-to-right.
        # We use Prepend for all libs. Since later Prepends go BEFORE earlier ones:
        #   1. First we Prepend deps (they'll end up after Firebase libs)
        #   2. Then we Prepend Firebase libs (they'll end up before deps)
        # Final order: [Firebase module .obj] → [Firebase libs] → [deps] → [system libs]
        # ============================================================================

        deps_base = lib_base + "deps/"

        # --- STEP 1: Prepend all dependency libraries (these go AFTER Firebase libs) ---
        # Listed in reverse dependency order (most-depended-on libs first, so they end up last)

        # Abseil logging and flags (most foundational)
        env.Prepend(LIBS=[File(deps_base + "absl_flags_commandlineflag_internal.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_flags_commandlineflag.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_flags_private_handle_accessor.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_flags_program_name.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_flags_config.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_flags_marshalling.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_flags_internal.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_flags_reflection.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_vlog_config_internal.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_leak_check.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_examine_stack.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_die_if_null.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_log_initialize.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_log_sink.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_log_entry.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_log_globals.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_log_internal_proto.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_log_internal_nullguard.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_log_internal_fnmatch.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_log_internal_globals.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_log_internal_format.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_log_internal_conditions.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_log_internal_check_op.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_log_internal_log_sink_set.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_log_internal_message.lib")])

        # Abseil random
        env.Prepend(LIBS=[File(deps_base + "absl_random_seed_gen_exception.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_random_internal_platform.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_random_internal_seed_material.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_random_internal_randen_hwaes_impl.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_random_internal_randen_hwaes.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_random_internal_randen_slow.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_random_internal_randen.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_random_internal_pool_urbg.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_random_seed_sequences.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_random_distributions.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_exponential_biased.lib")])

        # Abseil core
        env.Prepend(LIBS=[File(deps_base + "absl_strerror.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_bad_variant_access.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_bad_optional_access.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_debugging_internal.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_demangle_internal.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_symbolize.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_stacktrace.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_throw_delegate.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_log_severity.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_raw_logging_internal.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_malloc_internal.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_spinlock_wait.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_base.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_kernel_timeout_internal.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_graphcycles_internal.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_synchronization.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_int128.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_civil_time.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_time_zone.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_time.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_raw_hash_set.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_hashtablez_sampler.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_city.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_low_level_hash.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_hash.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_str_format_internal.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_string_view.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_strings_internal.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_strings.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_crc_cpu_detect.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_crc_internal.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_crc32c.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_crc_cord_state.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_cordz_functions.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_cordz_handle.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_cordz_info.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_cord_internal.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_cord.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_status.lib")])
        env.Prepend(LIBS=[File(deps_base + "absl_statusor.lib")])

        # zlib (compression, depended on by many)
        env.Prepend(LIBS=[File(deps_base + "zlibstatic.lib")])

        # Flatbuffers (serialization library)
        env.Prepend(LIBS=[File(deps_base + "flatbuffers.lib")])

        # libcurl and websockets (HTTP transport)
        env.Prepend(LIBS=[File(deps_base + "libuWS.lib")])
        env.Prepend(LIBS=[File(deps_base + "libcurl.lib")])

        # libuv (async I/O library required by RTDB)
        env.Prepend(LIBS=[File(deps_base + "uv_a.lib")])

        # LevelDB and snappy (required by RTDB for local caching)
        env.Prepend(LIBS=[File(deps_base + "snappy.lib")])
        env.Prepend(LIBS=[File(deps_base + "leveldb.lib")])

        # UPB (protobuf micro library used by gRPC)
        env.Prepend(LIBS=[File(deps_base + "utf8_validity.lib")])
        env.Prepend(LIBS=[File(deps_base + "utf8_range_lib.lib")])
        env.Prepend(LIBS=[File(deps_base + "upb_textformat_lib.lib")])
        env.Prepend(LIBS=[File(deps_base + "upb_message_lib.lib")])
        env.Prepend(LIBS=[File(deps_base + "upb_mem_lib.lib")])
        env.Prepend(LIBS=[File(deps_base + "upb_json_lib.lib")])
        env.Prepend(LIBS=[File(deps_base + "upb_base_lib.lib")])

        # gRPC and protobuf (required by Firestore and RTDB)
        env.Prepend(LIBS=[File(deps_base + "re2.lib")])
        env.Prepend(LIBS=[File(deps_base + "cares.lib")])
        env.Prepend(LIBS=[File(deps_base + "address_sorting.lib")])
        env.Prepend(LIBS=[File(deps_base + "protobuf-nanopb.lib")])
        env.Prepend(LIBS=[File(deps_base + "libprotobuf.lib")])
        env.Prepend(LIBS=[File(deps_base + "gpr.lib")])
        env.Prepend(LIBS=[File(deps_base + "grpc++.lib")])
        env.Prepend(LIBS=[File(deps_base + "grpc.lib")])

        # Firestore dependencies (required for firebase_firestore)
        env.Prepend(LIBS=[File(deps_base + "firestore_util.lib")])
        env.Prepend(LIBS=[File(deps_base + "firestore_protos_nanopb.lib")])
        env.Prepend(LIBS=[File(deps_base + "firestore_nanopb.lib")])
        env.Prepend(LIBS=[File(deps_base + "firestore_core.lib")])

        print("Firebase: Prepended " + str(103) + " explicit dependency libraries from deps/")

        # --- STEP 2: Prepend Firebase core libraries (these go BEFORE deps) ---
        # Since these Prepends happen AFTER the deps Prepends above,
        # they end up at the FRONT of the link line, before deps.
        # Internal Firebase libraries (required by Auth, Remote Config for HTTP transport)
        env.Prepend(LIBS=[File(lib_base + "firebase_rest_lib.lib")])
        # Core Firebase libraries
        env.Prepend(LIBS=[File(lib_base + "firebase_messaging.lib")])
        env.Prepend(LIBS=[File(lib_base + "firebase_functions.lib")])
        env.Prepend(LIBS=[File(lib_base + "firebase_auth.lib")])
        env.Prepend(LIBS=[File(lib_base + "firebase_firestore.lib")])
        env.Prepend(LIBS=[File(lib_base + "firebase_database.lib")])
        env.Prepend(LIBS=[File(lib_base + "firebase_remote_config.lib")])
        env.Prepend(LIBS=[File(lib_base + "firebase_analytics.lib")])
        env.Prepend(LIBS=[File(lib_base + "firebase_app.lib")])
        print("Firebase: Prepended core Firebase libraries")

        # ============================================================================
        # Windows system libraries required by Firebase SDK (Task-434)
        # Use LINKFLAGS to pass system libraries directly to linker (avoids SCons name mangling)
        # Reference: https://firebase.google.com/docs/database/cpp/start

        # Core Firebase libraries (all services)
        env.Append(LINKFLAGS=['advapi32.lib'])  # Windows advanced API (cryptography, registry)
        env.Append(LINKFLAGS=['ws2_32.lib'])    # Windows Sockets (CRITICAL: RTDB desktop uses REST API)
        env.Append(LINKFLAGS=['crypt32.lib'])   # Cryptography API (auth/tokens)

        # Realtime Database specific libraries
        env.Append(LINKFLAGS=['iphlpapi.lib'])  # IP Helper API (network operations)
        env.Append(LINKFLAGS=['psapi.lib'])     # Process Status API
        env.Append(LINKFLAGS=['Userenv.lib'])   # User Environment/Profile ( GetUserProfileDirectoryW)

        # Firestore specific
        env.Append(LINKFLAGS=['dbghelp.lib'])   # Debug Help (SymSetOptions, SymInitialize)

        # ICU libraries for timezone/locale functions (u_errorName, ucal_getTimeZoneIDForWindowsID)
        # Windows 10 1903+ uses icu.lib, older versions use icuuc.lib + icuin.lib
        env.Append(LINKFLAGS=['icu.lib'])       # Modern unified ICU library (Windows 10 1903+)

        # Additional system libraries needed by from-source Firebase SDK dependencies
        env.Append(LINKFLAGS=['Shell32.lib'])   # Shell API (SHGetFolderPathW)
        env.Append(LINKFLAGS=['Bcrypt.lib'])    # Windows Cryptography API (BCryptGenRandom)
        env.Append(LINKFLAGS=['Winhttp.lib'])   # HTTP Services (WinHttpOpen)
        env.Append(LINKFLAGS=['Shlwapi.lib'])   # Shell Lightweight API

        # ============================================================================
        # OpenSSL libraries (required by gRPC, libcurl, Firebase Auth/Database)
        # Firebase SDK uses system OpenSSL on Windows (not bundled BoringSSL)
        # OpenSSL installed at: C:\Program Files\OpenSSL-Win64
        # ============================================================================

        # Add OpenSSL library path (escaped for Windows backslashes)
        openssl_lib_path = "C:\\Program Files\\OpenSSL-Win64\\lib"
        env.Append(LINKFLAGS=['/LIBPATH:' + openssl_lib_path])
        print("Firebase: Added OpenSSL library path: " + openssl_lib_path)

        # OpenSSL crypto and SSL libraries
        env.Append(LINKFLAGS=['libcrypto.lib'])  # OpenSSL cryptographic functions (SHA1, EVP_*)
        env.Append(LINKFLAGS=['libssl.lib'])     # OpenSSL SSL/TLS functions (SSL_write, SSL_ctrl)

        # Legacy CRT functions required by some Firebase components
        # These resolve __imp__difftime64 and other UCRT functions
        env.Append(LINKFLAGS=['legacy_stdio_definitions.lib'])  # Legacy stdio functions
    else:
        print('WARN: Unsupported architecture for Firebase Windows: ' + env['arch'] + ' (only x86_64 supported)')
