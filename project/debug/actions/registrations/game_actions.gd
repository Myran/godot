# project/debug/actions/registrations/game_actions.gd
class_name GameDebugActions
extends RefCounted


static func register_all(registry) -> void:
	# TODO: Fix lambda function issues in static context
	# For now, game actions will be registered directly in debug_action_registry.gd
	pass
