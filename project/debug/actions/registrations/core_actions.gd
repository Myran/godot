# project/debug/actions/registrations/core_actions.gd
class_name CoreDebugActions
extends RefCounted


static func register_all(registry) -> void:
	# TODO: Fix lambda function issues in static context
	# For now, core actions will be registered directly in debug_action_registry.gd
	pass
