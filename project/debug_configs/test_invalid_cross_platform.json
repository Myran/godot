{
  "description": "TDD RED phase test config - contains platform-specific actions (should FAIL)",
  "type": "test_invalid_config",
  "actions": [
    "system.debug.registry_stats",
    "system.debug.test_desktop_functionality",
    "game.draft.upgrade_player",
    "system.debug.quit_application"
  ],
  "metadata": {
    "test_purpose": "TDD RED phase - validate that platform-specific actions are rejected",
    "expected_validation_result": "FAIL"
  }
}
